import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  card: {
    maxHeight: '300px',
  },
  media: {
    height: '200px',
  },
  content: {
    paddingTop: theme.spacing(0.5),
  }
}));